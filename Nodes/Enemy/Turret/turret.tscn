[gd_scene load_steps=4 format=3 uid="uid://c1wkqlvoevgnr"]

[ext_resource type="Texture2D" uid="uid://drvh6auph70yf" path="res://Assets/Enemy/Turret/turret_base.png" id="1_hpxcc"]
[ext_resource type="PackedScene" uid="uid://f268qaqp76cr" path="res://Nodes/Enemy/Cannon/cannon.tscn" id="2_iq3nj"]

[sub_resource type="GDScript" id="GDScript_6lndl"]
script/source = "extends EnemyBase

@export var enable_gravity : bool = true
#const SPEED = 300.0
#const JUMP_VELOCITY = -400.0
@onready var cannon: Cannon = $Cannon

func _ready() -> void:
	cannon.died.connect(func() :
		_on_died()
		print(\"turret died\")
	)

func _physics_process(delta: float) -> void:
	# Add the gravity.
	if not is_on_floor() and enable_gravity:
		velocity += get_gravity() * delta
	
	


	# Handle jump.
	#if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		#velocity.y = JUMP_VELOCITY

	# Get the input direction and handle the movement/deceleration.
	# As good practice, you should replace UI actions with custom gameplay actions.
	#var direction := Input.get_axis(\"ui_left\", \"ui_right\")
	#if direction:
		#velocity.x = direction * SPEED
	#else:
		#velocity.x = move_toward(velocity.x, 0, SPEED)
	#move_and_collide()
	move_and_slide()
"

[node name="Turret" type="CharacterBody2D" groups=["Enemy"]]
collision_layer = 256
collision_mask = 32
motion_mode = 1
script = SubResource("GDScript_6lndl")

[node name="Sprite2D" type="Sprite2D" parent="."]
position = Vector2(0, -39)
texture = ExtResource("1_hpxcc")

[node name="Cannon" parent="." instance=ExtResource("2_iq3nj")]
position = Vector2(0, -70.99999)
is_summoned = true

[node name="CollisionPolygon2D" type="CollisionPolygon2D" parent="."]
position = Vector2(-50, -1)
polygon = PackedVector2Array(97, 1, 79, -16, 79, -26, 71, -36, 26, -36, 19, -27, 19, -17, -1, 0)
